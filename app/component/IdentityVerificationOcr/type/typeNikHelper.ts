export type NikHelpersOptions = {
  NIK_CHAR_MAP: Record<string, string>;
  NIK_OCR_WHITELIST: string;
  NIK_DYNAMIC_SCALE_HEIGHT_THRESHOLD: number;
  NIK_DIGIT_CORRECTION_MAP: Record<string, string[]>;
};
